# Leave Management System

Full-stack leave management app with a React frontend and Node.js/Express backend.

## Structure

- `client/` - React frontend
- `server/` - Node.js backend

## Run Locally

Install dependencies in each app, then run them separately:

```bash
cd server
npm install
npm run dev
```

```bash
cd client
npm install
npm start
```
